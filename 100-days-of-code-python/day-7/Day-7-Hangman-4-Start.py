# Step 4

import random

stages = ['''
  +---+
  |   |
  O   |
 /|\  |
 / \  |
      |
=========
''', '''
  +---+
  |   |
  O   |
 /|\  |
 /    |
      |
=========
''', '''
  +---+
  |   |
  O   |
 /|\  |
      |
      |
=========
''', '''
  +---+
  |   |
  O   |
 /|   |
      |
      |
=========''', '''
  +---+
  |   |
  O   |
  |   |
      |
      |
=========
''', '''
  +---+
  |   |
  O   |
      |
      |
      |
=========
''', '''
  +---+
  |   |
      |
      |
      |
      |
=========
''']

end_of_game = False
word_list = ["ardvark", "baboon", "camel"]
chosen_word = random.choice(word_list)
word_length = len(chosen_word)

# TODO-1: - Crie uma variável chamada 'vidas' para acompanhar o número de vidas restantes.
# Defina 'vidas' para igual a 6.
vidas = 6

# Testing code
print(f'Pssst, the solution is {chosen_word}.')

# Create blanks
display = []
for _ in range(word_length):
    display += "_"

while not end_of_game:
    guess = input("Guess a letter: ").lower()

    # Check guessed letter
    for position in range(word_length):
        letter = chosen_word[position]
        # print(f"Current position: {position}\n Current letter: {letter}\n Guessed letter: {guess}")
        if letter == guess:
            display[position] = letter

# TODO-2: - Se o palpite não for uma letra na palavra escolhida,
# Em seguida, reduza 'vidas' em 1.
# Se as vidas chegarem a 0, o jogo deve parar e exibir "Você perde".

    if guess not in chosen_word:
        vidas -= 1
        if vidas == 0:
            end_of_game = True
            print("Você perdeu.")

    # Join all the elements in the list and turn it into a String.
    print(f"{' '.join(display)}")

    # Check if user has got all letters.
    if "_" not in display:
        end_of_game = True
        print("You win.")

# TODO-3: - imprimir a arte ASCII a partir de 'etapas' que corresponde ao número atual de 'vidas' que o usuário ainda tem.
    print(stages[vidas])
